export interface Dog {
  id: string;
  name: string;
  breed: string;
  age: number;
  img: string;
  zip_code: string;
  isFavorite: boolean;
}

export interface User {
  id: string;
  name: string;
  token: string;
}
